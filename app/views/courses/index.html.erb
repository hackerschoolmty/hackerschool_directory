<p id="notice"><%= notice %></p>

<h1>Cursos de la generación <%= @generation.name %></h1>

<%= link_to "Nuevo Curso", new_generation_course_path(@generation) %>
<table class="table table-striped">
  <thead>
    <tr>
      <th>Name</th>
      <th>Dificulty</th>
      <th>Generation</th>
      <th colspan="2"></th>
    </tr>
  </thead>

  <tbody>
    <% @courses.each do |course| %>
      <tr>
        <td><%= course.name %></td>
        <td><%= course.dificulty %></td>
        <td><%= course.generation.name %></td>
        <td><%= link_to 'Edit', edit_generation_course_path(@generation.id, course.id) %></td>
        <td><%= link_to 'Destroy', generation_course_path(@generation, course), method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>


